# Copy to .env locally. Do not commit real values.
# XAI_API_KEY is injected in preview/production; set locally only if you run Grok/Imagine yourself.
XAI_API_KEY=
DATABASE_URL=
BETTER_AUTH_SECRET=
FFMPEG=/usr/local/bin/ffmpeg

# Owner — production never elects the first signup.
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_USER_ID=
BOOTSTRAP_SECRET=

# Payments — public checkout requires all three. HMAC + amount/currency match before grant.
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=
NOWPAYMENTS_IPN_URL=
